Place the official Public Data Portal OpenAPI logo here as:

- /public/logos/public-data-api-logo.png

The footer component already references that path and falls back to text if the file is not present.
